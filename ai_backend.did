type CanisterInfo = record {
  id : nat64;
  updated_at : nat64;
  name : text;
  canister_id : text;
  created_at : nat64;
  user_id : text;
};
type ErrorResponse = record { errors : vec record { text; text } };
type Result = variant { Ok : bool; Err : ErrorResponse };
type Result_1 = variant { Ok : CanisterInfo; Err : ErrorResponse };
service : {
  analyze_canister : (text) -> (text);
  clear_storage : () -> ();
  delete_canister : (text) -> (Result);
  list_canisters : () -> (vec CanisterInfo) query;
  register_canister : (text, text) -> (Result_1);
  update_canister : (text, text) -> (Result);
  view_canister : (text) -> (Result_1) query;
}
